# pytest.ini
[pytest]
minversion = 6.0
addopts = -ra -q
testpaths =
    api
    integration
    models
    utils

# enable verbose output
verbose = true
# enable logging to a file
log_file = tests.log